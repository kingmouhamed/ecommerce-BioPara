# إضافة معلمات cache-busting للصور

## تمت الإصلاحات التالية:

### 1. **تحديث الشعار (Logo)**
- إضافة `?v=2` إلى مسار الشعار في Header.tsx
- إضافة `priority` لتحسين الأداء

### 2. **تحديث صور المنتجات**
- إضافة `?v=2` إلى جميع مسارات الصور في products.js
- تحسين ProductCard.jsx مع `sizes` attribute

### 3. **إنشاء أدوات مساعدة**
- ملف `utils/imageUtils.js` للتعامل مع cache-busting

## الخطوات التالية:

1. **نشر التغييرات** إلى Vercel
2. **مسح التخزين المؤقت** في المتصفح
3. **التحقق من الصور** على الموقع المنشور

## أوامر مسح التخزين المؤقت:

### للمتصفح:
- `Ctrl + Shift + R` (Windows)
- `Cmd + Shift + R` (Mac)

### لـ Vercel (إذا لزم الأمر):
```bash
vercel --prod
```

## ملاحظات:
- جميع الصور الآن تستخدم `?v=2` لفرض التحديث
- تم تحسين أداء التحميل مع `priority` و `sizes`
- الكود جاهز للنشر
